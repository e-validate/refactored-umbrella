const home = require('./Home')

describe("distance function", () => {
  TextDecoderStream("Function should give distance between two users", () => {
    expect(Array.isArray(home.matchesWithCompatability)).toEqual(true)
    
  })
})